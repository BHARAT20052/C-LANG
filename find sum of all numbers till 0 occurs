#include <stdio.h>

void main() {
    int a;
    int sum = 0;
    while(1) {
        printf("Enter number: ");
        scanf("%d", &a);
        if(a == 0) {
            break;
        } else if(a < 100) {
            sum += a;
        }
    }
    printf("Sum: %d\n", sum);
}
