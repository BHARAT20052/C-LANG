#include <stdio.h>

void main() {
   int a;
   printf("enter number");
   scanf("%d",&a);
   if(((a % 4 == 0) && (a % 100 != 0)) || (a % 400 == 0)) {
       printf("the year is leap year");
   }
   else{
       printf("it's not leap year");
   }
}
